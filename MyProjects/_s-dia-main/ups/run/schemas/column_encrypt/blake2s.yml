openapi: 3.1.0
info:
  title: BLAKE2s Schema
  version: 1.0.0
  description: BLAKE2s 해시 함수를 사용한 암호화 스키마

components:
  schemas:
    blake2s:
      type: object
      required:
        - function
        - parameters
      properties:
        function: { const: blake2s }
        parameters:
          type: object
          required:
            - columns
          properties:
            columns:
              $ref: '../definitions.yml#/components/schemas/column_name_or_list'
            length:
              type: integer
              minimum: 1
              maximum: 32
            salt:
              type: string
            salt_position:
              type: string
              enum: [prefix, suffix]
            encoding:
              type: string
              enum: [hex_lower, hex_upper, base64]
            output_columns:
              $ref: '../definitions.yml#/components/schemas/column_name_or_list'
